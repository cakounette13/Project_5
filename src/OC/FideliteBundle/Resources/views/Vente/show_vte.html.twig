{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vente</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vente.id }}</td>
            </tr>
            <tr>
                <th>Datevente</th>
                <td>{% if vente.dateVente %}{{ vente.dateVente|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Montantventettc</th>
                <td>{{ vente.montantVenteTtc }}</td>
            </tr>
            <tr>
                <th>Pointfidelite</th>
                <td>{{ vente.pointFidelite }}</td>
            </tr>
            <tr>
                <th>Client</th>
                <td>{{ vente.client }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vente_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vente_edit', { 'id': vente.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
