{% extends "base.html.twig" %}

{% block body %}
    <div class="fos_user_user_show">
        <div class="col-md-offset-1 col-md-10 col-lg-offset-1 col-lg-10 col-sm-offset-1 col-sm-10 col-xs-offset-1 col-xs-10 text-center">
            <h1>Profil</h1>
            <div class="col-xs-12">
                <table class="table table-bordered table-condensed table-striped">
                    <thead>
                    <tr>
                        <th class="text-center">Utilisateur</th>
                        <th class="text-center">Email</th>
                    </tr>
                    </thead>
                    <tbody>
                    <tr>
                        <td>{{ user.username }}</td>
                        <td>{{ user.email }}</td>
                    </tr>
                    </tbody>
                </table>
            </div>
            <div class="row btn-group-vertical">
                <a class="btn btn-default" href={{ path("security_edit_profile") }}>Changer de nom de compte</a>
                <a class="btn btn-default" href={{ path("security_change_password") }}>Modifier votre mot de passe</a>
                <a class="btn btn-default" href={{ path("accueil") }}>Retour à l'accueil</a>
            </div>
        </div>
    </div>
{% endblock %}